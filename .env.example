# Mapbox (required for the interactive map)
# Create web/.env locally or set in Netlify → Environment variables
VITE_MAPBOX_TOKEN=

# Contact page background video (optional — file is not in Git due to size)
# Upload the MP4 to Netlify, Cloudinary, S3, etc. and paste the URL here.
VITE_CONTACT_VIDEO_URL=
